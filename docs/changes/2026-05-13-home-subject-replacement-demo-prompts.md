# CHG-20260513-016 首页主体替换展示区演示提示词接入

## 背景

首页“视频主体替换 - 精准定向修改”展示区已经接入替换前视频、参考图和替换后视频，还需要把演示提示词从占位 `xxx` 替换成真实文案。

## 改动

- `原视频中要替换的对象` 改为 `画面中央带眼镜的女主播`。
- `替换成` 改为 `欧美人`。

## 影响范围

- `frontend/templates/series/series_list.html`

## 验证

- `python3 manage.py check`
- `mvn -DskipTests compile`
- 重启前端 `8000` 与后端 `8081`
- 浏览器打开首页检查文案展示
