# CHG-20260513-010 首页主体替换新功能徽标样式

## 背景

首页主体替换按钮右上角的黄色 `NEW` 标签视觉上过于突兀，和按钮整体风格不够统一。

## 改动

- 将黄色角标改成按钮内部的 `NEW` 微型徽标。
- 使用低对比深色半透明背景和细描边，降低突兀感。
- 保留新功能提示，但不抢主体按钮视觉焦点。

## 影响范围

- `frontend/templates/series/series_list.html`

## 验证

- `python3 manage.py check`
- 重启前端 `8000` 与后端 `8081`
